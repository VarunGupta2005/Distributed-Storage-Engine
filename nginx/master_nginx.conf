events { 
    worker_connections 1024; 
}

http {
    upstream master_cluster {
        # Points to C++ Master Nodes running locally
        server host.docker.internal:8081 max_fails=2 fail_timeout=10s;
        server host.docker.internal:8082 max_fails=2 fail_timeout=10s;
    }

    server {
        listen 80; 

        location / {
            proxy_pass http://master_cluster;
            
            # Automatic Failover settings
            proxy_next_upstream error timeout invalid_header http_500 http_502 http_503;
            proxy_connect_timeout 2s;
        }
    }
}